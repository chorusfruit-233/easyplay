# The Gradle wrapper (gradlew, gradlew.bat, gradle-wrapper.jar) is intentionally
# tracked: it pins the Gradle version the project builds with, so a fresh clone
# can build without regenerating it via `flutter create`.
/.gradle
/captures/
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
